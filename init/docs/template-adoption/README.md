# Template Adoption Reports

This folder stores reports created when `repo-template` is adopted into an existing repository.

Reports should list:

- files created
- files skipped because they already existed
- manual merge recommendations
- follow-up checks

Adoption reports are documentation, not source-of-truth project policy. The target repo's actual files remain authoritative.
