# Memory

Durable facts and answers for future humans and agents. Keep this short,
concrete, and curated — it is reread on demand, so prune it rather than letting
it grow. For in-flight work use `../TASKS.md`; for chronology use `journal.md`.

## Stable Facts

<!-- e.g., "Postgres 16 is the only supported database." -->

## Repeated Decisions

<!-- e.g., "Always use parameterized queries, never string interpolation." -->

## Known Traps

<!-- e.g., "The test runner hangs if port 5432 is already in use." -->

## Preferences

<!-- e.g., "Prefer async/await over raw Promise chains." -->

## Compiled Q&A

Recurring questions and their stable answers, so they are not rediscovered in
chat or issues. Link to deeper sources when an answer grows.

<!-- Q: How do we run a single integration test? -->
<!-- A: `./scripts/check.sh --only integration` -->

> Chronological notes belong in `journal.md`. Current work belongs in
> `../TASKS.md`. This file is for facts that stay true across sessions.
