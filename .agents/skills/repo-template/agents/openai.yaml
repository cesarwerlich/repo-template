interface:
  display_name: "Repo Template"
  short_description: "Repo-specific patterns and workflows for repo-template"
  default_prompt: "Use the repo-template repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true